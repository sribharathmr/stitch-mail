# Stitch Mail — Client Environment Variables
# For local development, the Vite proxy handles API routing (no env needed)
# For production deployment (e.g. Vercel), set this to your Railway backend URL

VITE_API_URL=https://your-railway-app.up.railway.app/api
